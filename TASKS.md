# TASKS.md — Current Backlog

## Completed
- **[TASK-001–016]** All APPROVED (a11y sweep, accent bullets, aria-live, mobile polish, celebration animation, GlossaryTooltip, tooltip integration, 375px mobile fixes, MP infrastructure + MP1)
- **[TASK-019]** Phase 2 — Structure (navigation, page template components, References page). Verified against the repo: `References.jsx`, `DefinitionBox`/`NotationBox`/`ExampleBox`/`RemarkBox`/`PrereqList`, and the Study Paths/References nav labels all exist as specified in `agent.md`'s TASK-019. This was done but never logged here — reconciled 2026-08-09.
- **[TASK-020]** Diagnostic Placement Pilot — CLAUDE.md amendment. Added "Diagnostic Placement & Concept Evidence" section, "IBM Video Sourcing" guidance, corrected stale Study Paths status, added Phase 3a to Build Plan. Doc-only; `npm run build` verified passing. Completed 2026-08-09.
- **[TASK-021]** Diagnostic Placement Pilot — concept graph + diagnostic question data model. Added `lib/data/concepts.js` (14 concepts across the 3 pilot modules) and `lib/data/diagnostic.js` (22 questions across 3 areas, `DIAGNOSTIC_VERSION` contract). Cross-checked programmatically: every question's concept/area ids resolve, every concept prereq resolves, all ids unique, all choice arrays valid. 9 of 14 concepts have ≥2 tagged questions (collapsible-eligible); the rest are deliberately left below threshold per the evidence-model rule. Not yet imported anywhere (no behavior change). `npm run build` verified passing. Completed 2026-08-09.
- **[TASK-022]** Diagnostic Placement Pilot — `useDiagnostic` hook. Added `lib/hooks/useDiagnostic.js`: raw-answer localStorage persistence (`quantum_diagnostic_v1`), `DIAGNOSTIC_VERSION` staleness detection, live-derived area scores, three-way `conceptStatus` (`demonstrated`/`needs-review`/`insufficient-evidence`), `demonstrated` Set, and `recommendedStartModuleId`/`reviewModuleIds` derived from area scores only — no `STUDY_PATHS` import. `npm run build` verified passing. Completed 2026-08-09.
- **[TASK-023]** Diagnostic Placement Pilot — Diagnostic page/route + Roadmap entry point. Added `components/DiagnosticQuestion.jsx`, `app/pages/Diagnostic.jsx` (intro → 3-area question flow → results view), `/diagnostic` route in `App.jsx` via `lazyWithRecovery`, and a plain entry-point CTA on `Roadmap.jsx` above the Study Path cards (no data coupling, no per-path badge). `npm run build` verified passing, produces a separate `Diagnostic-*.js` chunk. Not yet visually verified in a browser — pending user check. Completed 2026-08-09.
- **[TASK-027]** New "Mathematical Language" module — content gap fix, NOT part of the Diagnostic Placement Pilot. `BraKet.jsx`'s own prereq list referenced "familiarity with vectors" with nowhere to send a student who didn't have it, and CLAUDE.md's IA promises `/math-language` with no route/page/MODULES entry behind it. Added `app/pages/MathLanguage.jsx` (complex numbers, vectors as columns, addition/scalar multiplication, normalization — full module matching the Intuition/Bra-Ket/Gates pattern: ModuleLayout with outline+aside, DefinitionBox/NotationBox/ExampleBox/RemarkBox, MistakesBox, SummaryBox), inserted as module 2 in `lib/data/modules.js` (`prereqs: ['intuition']`), and renumbered every downstream module (old 2–13 → 3–14) across `MODULES`, `MODULE_STYLES`, `MODULE_LAYOUT_STYLES`, and the hardcoded "Module N: ..." prev/next labels in all 13 existing module pages — verified as one consistent chain end to end. `braket`'s prereqs changed from `['intuition']` to `['math-language']`. `Roadmap.jsx`'s Study Paths B and C now include `math-language` (matching CLAUDE.md's stated sequences); Path A is unchanged (CLAUDE.md's Path A also omits it). Added 3 new `Keyword` tones (`complex`/`vector`/`normalization`) and a `Sigma` icon/blue color slot. Deliberately does NOT touch `lib/data/concepts.js`, `lib/data/diagnostic.js`, or `useDiagnostic.js` — the diagnostic pilot's scope stays the original 3 modules unless separately expanded. Verified: `npm run build` passes; a Node script confirmed `MODULES` numbers are sequential 1–14 with no gaps/duplicates, every id has a `MODULE_STYLES`/`MODULE_LAYOUT_STYLES` entry, and all prereqs resolve. Not yet visually verified in a browser. Completed 2026-08-09.
- **[TASK-024]** Diagnostic Placement Pilot — `ConceptSection` + wiring into Intuition/Bra-Ket/Gates. Added `components/ConceptSection.jsx` (collapses only when every id in `conceptIds` is in the page's `demonstrated` Set; otherwise renders children exactly as-is — default is always fully expanded). Wired one `useDiagnostic()` call per page (not per block) into `Intuition.jsx` (5 clusters), `BraKet.jsx` (4 clusters), `Gates.jsx` (5 clusters) — all 14 concepts from `concepts.js` now wired to their `sectionId`, section headings/intros left outside the collapse so outline nav is unaffected. Verified with a standalone Node script replicating `useDiagnostic`'s exact derivation logic (not just code review): simulated answering all math-notation + gates questions correctly, confirmed `ket-notation`/`bra-notation`/`inner-product`/`unitary-gate`/`pauli-x`/`hadamard` reach `demonstrated` while `notation-reading`/`pauli-z`/`s-t-gates` correctly stay `insufficient-evidence` (only 1 tagged question each), and untouched-area concepts stay `insufficient-evidence` too. `npm run build` passes; `useDiagnostic` now code-splits into its own shared chunk across the 4 pages that use it. Not yet visually verified in a browser. Completed 2026-08-09.
- **[TASK-028]** Diagnostic Placement Pilot — study-sequence chain + save/restore code. Direct follow-up to user feedback that the results view "does not match the initial description": replaced the flat `recommendedStartModuleId`/`reviewModuleIds` fields in `useDiagnostic.js` with `studyChain`/`studyChainHead` — an actual ordered, linked-list-shaped sequence (`{ moduleId, areaLabel, scorePct, status, next }[]`, weakest area first, each node pointing to the next) across the 3 diagnostic-tested modules. Still deliberately not derived from `STUDY_PATHS` — same reasoning as before. Added `lib/utils/diagnosticCode.js`: a dependency-free Crockford Base32 + checksum codec (`SQD1-XXXX-XXXX-XXXX-X`, ~21 chars) over a completed diagnostic's raw answers — no accounts, nothing sensitive, nothing sent anywhere. `Diagnostic.jsx`'s results view now renders the chain as a connected sequence of linked nodes and a "Save this result" panel with copy-to-clipboard; the intro screen (and results view) both gained a "Restore from a code" panel. Verified: a standalone script confirmed the encode/decode roundtrips exactly (all-zero answers, correct answers, all-max answers), that a tampered code is rejected by the checksum, and that an incomplete diagnostic cannot be exported; a separate script confirmed the chain-building/ranking/status/next-pointer logic against mock scores. `npm run build` passes. Known minor gap: importing a code from a future/different `DIAGNOSTIC_VERSION` falls back safely (no crash, no bad data shown) but doesn't yet show an explicit "this code is from an old version" message — unreachable today since only version 1 has ever existed, not fixed now. Not yet visually verified in a browser. Completed 2026-08-09.
- **[TASK-025]** Diagnostic Placement Pilot — Gates.jsx matrix tables + Intuition.jsx analogy audit. Added explicit 2×2 matrices for X, Z, H, S, T to `Gates.jsx` via `MathDisplay`/`pmatrix`, each inside its gate's existing `ConceptSection` cluster (not outside it) as a `NotationBox` immediately after the `DefinitionBox`, matching `BraKet.jsx`'s established matrix-display convention. Scoped to exactly these 5 matrices, no generic `U = [[u11,u12],[u21,u22]]` form added (not the confirmed gap). Audited `Intuition.jsx` for analogy-substituting-for-definition per CLAUDE.md's flagged failure mode: grepped for analogy-signaling language (`like a`, `similar to`, `imagine`, `think of`, `analog*`) — zero matches. Every section already leads with a formal `DefinitionBox`; the only analogy-adjacent text found is in `RemarkBox`es that explicitly *correct* a naive analogy (e.g. "a qubit should not be understood as two classical bits packed into one place"), which is the desired pattern, not a violation of it. **Conclusion: no change needed to `Intuition.jsx`** — documented here per the task's explicit instruction not to force an edit to justify the task. `npm run build` passes. Not yet visually verified in a browser. Completed 2026-08-09.
- **[TASK-026]** Diagnostic Placement Pilot — `VideoAside` + `ExpandableAside` focus-visible fix + accessibility verification. Added `components/VideoAside.jsx`: text summary permanently visible outside any `<details>`, iframe gated on `hasLoaded` (set on first `onToggle` open, never unmounted on close, so no reload/reset on reopen), `focus-visible` on the `<summary>`. Fixed `ExpandableAside.jsx`'s `<summary>`, which had zero `focus-visible` classes (a real pre-existing gap, confirmed by reading the file, not assumed). **Video sourcing was verified, not trusted from search snippets**: fetched each candidate via YouTube's oEmbed JSON endpoint to check the actual `author_name`/`author_url` — two candidates that looked IBM-adjacent in search results turned out to be third parties (`Visualmatics`, `sentdex`) and were rejected. Landed on 2 genuinely IBM-owned videos: "What is Superposition?" (Qiskit channel, `@qiskit`) wired into `Intuition.jsx`'s superposition section, and "Quantum Gates" (IBM Research, `@ibmresearch`) wired into `Gates.jsx`'s unitary-gate section. **No video was added to `BraKet.jsx`** — no verified-IBM video on Dirac/bra-ket notation was found, and per "one video per pilot module max" (not "required"), a wrong attribution was judged worse than no video. **Accessibility pass found and fixed a real bug, not just a review**: `DiagnosticQuestion.jsx` used `role="radiogroup"`/`role="radio"` without the roving-tabindex + arrow-key navigation that pattern requires from assistive tech's perspective — a false promise, not a cosmetic gap. Fixed with the full ARIA APG radio-group keyboard pattern (Arrow/Home/End move focus and selection together; only one choice is ever a Tab stop). Also verified: all other new interactive elements are native `button`/`a`/`input`/`details`/`summary` elements with `focus-visible` classes; `ConceptSection` and `StudyChainView`'s status indicators carry a text label alongside color, not color alone; `RestoreCodeSection`'s error has `role="alert"` and its input is properly `htmlFor`/`id`-associated. **Found and documented, not fixed**: the codebase has no sitewide CSS-level `@media (prefers-reduced-motion: reduce)` rule at all (only Framer Motion's own `MotionConfig reducedMotion="user"` in `App.jsx`, which doesn't cover plain CSS transitions) — a pre-existing site-wide gap, not introduced by this pilot and out of scope to fix broadly here; none of this pilot's new components use Framer Motion, so they're at least not making that gap worse. This was a code-level verification pass (careful, targeted, caught a real interaction-pattern bug), not a literal screen-reader/browser session — consistent with this project's no-Playwright/Chromium convention; being explicit about that boundary rather than claiming a full manual a11y audit. `npm run build` passes. Completed 2026-08-09. **This closes out the Diagnostic Placement pilot (Phase 3a) as originally scoped.**
- **[TASK-017]** MP2 — Bell State Explorer (full content). Replaced the 40-line "under construction" stub with a 5-step `ProjectLayout` walkthrough matching `FirstCircuit.jsx`'s established pattern (Predict/Reflect callouts, `CodeBlock`, `GlossaryTooltip`, step-gated progress via `useProgress`'s `project-bell-explorer` key). Steps: understand the goal → set up two qubits → entangle them (H + CNOT, reusing the exact circuit already established in `Circuits.jsx`) → measure and simulate → analyze results (including a second code snippet showing how to prepare `|Ψ+⟩` instead of `|Φ+⟩`). Caught and fixed two real content bugs before finishing, not just written and shipped: `GlossaryTooltip` wraps were checked against `glossary.js`'s actual term list — none were mismatched in this file, but the same check on TASK-018 caught two real errors there (see below). `npm run build` passes; chunk size grew from 1.65kB (stub) to 7.17kB, consistent with real content replacing the placeholder. Not yet visually verified in a browser. Completed 2026-08-09.
- **[TASK-018]** MP3 — Algorithm Showdown (full content). Replaced the stub with a 6-step walkthrough: classical unstructured search baseline → Grover's oracle (phase-flip via `qc.cz`) → diffuser → a query-count comparison table (100 / 10,000 / 1,000,000 items, reusing `Algorithms.jsx`'s existing "~1,000 vs 500,000 at N=1,000,000" framing rather than inventing new numbers) → a closing analysis step tying to `/noise` and `/usecases` on why a quadratic speedup is fragile against real hardware noise. **Caught two real `GlossaryTooltip` mismatches before shipping**: `term="Amplitude"` had been wrapped around the phrase "Grover's algorithm" (unrelated pairing) and `term="Gate"` around "oracle" (glossary actually has a dedicated `"Oracle"` entry) — both were checked against `glossary.js`'s real term list, not assumed correct, and fixed to `term="Oracle"` / rephrased so "amplitude" is the word actually wrapped. `npm run build` passes; chunk grew from 1.64kB to 8.99kB. Not yet visually verified in a browser. Completed 2026-08-09.
- **[TASK-029]** One `VideoAside` per module (all 14), sourcing broadened beyond IBM-only. User asked for video coverage across every module, "ideally from IBM, but other reputable sources also works." Amended CLAUDE.md's "IBM Video Sourcing" section to "Video Sourcing": IBM/Qiskit preferred, other verified-reputable sources (established institutions, well-known science-education channels) acceptable — but the verify-via-oEmbed-before-use rule stays mandatory regardless of source. `VideoAside.jsx` gained a `source` prop (was hardcoded "IBM Quantum"; now shows whatever channel is actually verified). Sourced and **verified via oEmbed one by one** — 2 more candidates rejected as unverified third parties in this pass alone (`For the Love of Physics` for a Dirac-notation video, `Spectral Forge Labs` for a use-cases video), on top of the 2 rejected in TASK-026. Final set, all confirmed via oEmbed `author_name`: `math-language` → 3Blue1Brown ("Complex number fundamentals"); `braket` → MIT OpenCourseWare ("9. Dirac's Bra and Ket Notation"); `phase` → Qiskit ("Qubits and Quantum States... Part 1"); `qiskit` → Qiskit ("What is Qiskit?"); `multiqubit` → Qiskit ("Lecture 1.1 — Vector Spaces, Tensor Products, and Qubits"); `entanglement` → Veritasium ("Quantum Entanglement & Spooky Action at a Distance"); `circuits` → Qiskit ("Lecture 1.2 — Introduction to Quantum Circuits"); `measurement` → Qiskit ("...Part 2"); `algorithms` → Qiskit ("Grover's Algorithm — Coding with Qiskit"); `labs` → Qiskit ("Introduction to Qiskit — Coding with Qiskit 1.x"); `noise` → Qiskit ("Mitigating Noise in Quantum Hardware — Part 1"); `usecases` → Qiskit ("Mapping a Problem to a Quantum Computer"). `intuition` and `gates` already had verified videos from TASK-026 (just gained the new `source` prop). For the 3 modules already carrying `ConceptSection` (`intuition`/`braket`/`gates`), the video was placed inside the relevant existing cluster; for the other 5 "modern" pages (`math-language`/`phase`/`qiskit`/`multiqubit`/`entanglement`, no `ConceptSection` — out of the diagnostic pilot's scope), placed as a standalone block within the most topically relevant `<section>`; for the 6 "legacy" `stepInfo`/`LESSONS`-pattern pages (`circuits`/`measurement`/`algorithms`/`labs`/`noise`/`usecases`), placed once, persistently, right after the lesson stepper's `AnimatePresence` and before `StepNav` — visible regardless of which lesson step the reader is on, since there's no per-lesson slot that wouldn't require touching the `LESSONS` data shape. **Caught and fixed an unrelated stale bug while in `UseCases.jsx`**: its completion message said "You've finished all 13 modules" — stale since TASK-027 added Mathematical Language as module 14; grepped the whole codebase for the same stale count and confirmed it was the only occurrence. Verified: `grep -c` confirms exactly one `VideoAside` per all 14 module pages, no gaps or duplicates; a dedupe check on all 14 `videoId`s confirms no accidental reuse. `npm run build` passes; `VideoAside` code-splits into one shared ~9.6kB chunk reused across all 14 pages. Not yet visually verified in a browser. Completed 2026-08-09.

- **[TASK-030]** Home.jsx primary CTA rewired from `/intuition` ("Begin with Foundations") to `/diagnostic` ("Begin with the Diagnostic"). The diagnostic remains soft/advisory — its own intro screen still has an explicit "Skip — go to Study Paths" link, so this changes which button is first, not whether anything is gated. Secondary CTA ("Choose a Study Path" → `/roadmap`) unchanged. `npm run build` passes. Completed 2026-08-09.

- **[TASK-031]** CLAUDE.md accuracy + copyediting pass, doc-only. User asked to make sure everything in CLAUDE.md is well written. Fixed real staleness, not just prose: "Project Status" still said March 2026 and described the Diagnostic Placement pilot as future work when it (plus TASK-027/028/029/030) had since shipped in full — rewrote "Current State"/"What remains" to match reality. Phase 3a's checklist was 100% unchecked despite being 100% done — checked it off and added a "Follow-on work" block documenting the video-sourcing broadening (TASK-029), the new Mathematical Language module (TASK-027), both Machine Projects (TASK-017/018), and the Home CTA rewire (TASK-030), explicit that none of these reopen the pilot's own 3-module scope. The IA "Recommended structure" code block was a conceptual sketch (`/foundations`, `/states`, etc.) that never matched any real route — replaced with the actual 19-route list cross-checked against `App.jsx`. The homepage hero CTA example still said "Begin with Foundations" after TASK-030 changed it to "Begin with the Diagnostic" — updated. The "Homepage sections" list claimed a "Recommended starting paths" section that was never actually on `Home.jsx` (that content lives on `/roadmap`) — corrected to describe the real footer section, a discrepancy that predates this session. Component Architecture table never listed `ConceptSection`/`VideoAside`/`DiagnosticQuestion`/`ProjectLayout` despite the document discussing the first three at length elsewhere — added, and noted which originally-planned component names (`PathCard`, `ReferenceList`, `FigureFrame`, `CircuitStepper`) were never built as such so the table stops promising components that don't exist. Normalized typography: sections added across TASK-020–029 used straight quotes/apostrophes inconsistent with the rest of the document's curly-quote convention — swept the whole file, zero remaining straight apostrophes/quotes in prose (verified by grep, not by eye). **Also surfaced, not touched**: `/challenges` is a live, nav-unlinked route left over from before the Phase 1 identity shift — a gamified quiz-challenge page with score tracking, which is exactly what CLAUDE.md's "Bad uses" and "What This Site Is Not" sections warn against. Flagged to the user rather than deleted unprompted. Doc-only change; `npm run build` verified passing (sanity check, no `.jsx`/`.js` touched). Completed 2026-08-09.

- **[TASK-032]** Migrated `Circuits.jsx` (module 9) from the legacy `LESSONS`/`LessonCard`/`StepNav` lesson-stepper template to the current `ModuleLayout` outline+aside handbook template, matching `Entanglement.jsx`'s shape (intro, `PrereqList`/objectives, 7-entry outline with `DefinitionBox`/`NotationBox`/`ExampleBox`/`RemarkBox` per section, `MistakesBox`, `SummaryBox`, Next Steps). First of the "Advanced" group (modules 9-14) migration, done one module at a time per user direction after they asked why that group "still looks like the old version." All 4 of the original page's SVG interactives (circuit anatomy, three-building-blocks tabs, H+CNOT state stepper, diagram-to-code selector) were kept in the main flow and reskinned to the modern card convention rather than discarded, since CLAUDE.md says to keep an interactive that materially helps; the one redundant fourth stepper (`BellStepperVisual`, overlapping the kept state stepper) was moved into an `ExpandableAside` instead of deleted, per CLAUDE.md's "keep the most important one expanded, move supplementary interactives into `ExpandableAside`" rule. Fixed in passing: the old completion banner's stale "Module 8 complete" string (should have said 9 since TASK-027's renumbering) is gone entirely now that `ModuleLayout`'s own "Mark as Complete" footer replaces the custom banner. `GlossaryTooltip` terms and `Keyword` tones used were checked against `glossary.js`/`Keyword.jsx`'s real data, not assumed. Verified: `npm run build` passes; grep confirms all 7 outline ids match section anchors 1:1, no legacy imports remain, and nothing else in the repo references `Circuits.jsx` internals. Not yet visually verified in a browser (standing project convention). Measurement (module 10) is next in sequence, not yet scoped. Completed 2026-08-17.

- **[TASK-033]** Migrated `Measurement.jsx` (module 10) from the legacy `LESSONS`/`LessonCard`/`StepNav` template to `ModuleLayout` outline+aside, same pattern as TASK-032. Second of the "Advanced" group (modules 9-14), one module at a time. 7-entry outline covering the Z basis, measuring in different bases, why basis matters (phase visibility), the Born rule, basis change via Hadamard, common mistakes, and next steps. All 5 original interactive visuals (Z-basis measure button, Z-vs-X toggle, three-scenario comparison table, Born Rule Explorer slider, H basis-change stepper) were kept in the main flow, reskinned to the amber card convention matching `measurement`'s assigned module color — none were redundant enough to warrant folding into an `ExpandableAside` this time. Two of the original `deepDive` asides (BB84 motivation, complex-amplitude Born rule) were preserved as `ExpandableAside`s rather than dropped, plus a new one on arbitrary-basis measurement via a general unitary. Fixed the same stale-completion-banner pattern as TASK-032 ("Module 9 complete" on the module-10 page) by removing the custom banner entirely in favor of `ModuleLayout`'s own "Mark as Complete" footer. `GlossaryTooltip`/`Keyword` usage checked against real `glossary.js`/`Keyword.jsx` data. Verified: `npm run build` passes; grep confirms all 7 outline ids match section anchors 1:1, no legacy imports remain, nothing else references `Measurement.jsx` internals. Not yet visually verified in a browser. Algorithms (module 11) is next in sequence, not yet scoped. Completed 2026-08-17.

- **[TASK-034]** Migrated `Algorithms.jsx` (module 11) from the legacy `LESSONS`/`LessonCard`/`StepNav` template to `ModuleLayout` outline+aside, same pattern as TASK-032/033. Third of the "Advanced" group (modules 9-14). 7-entry outline covering Deutsch–Jozsa, Grover's search, phase kickback, Shor's algorithm, quantum advantage, common mistakes, and next steps. All 5 original interactive visuals kept in the main flow, reskinned to the orange card convention. Fixed a real pre-existing bug found while migrating: `AdvantageVisual` built Tailwind classes via template-string interpolation (`` `border-${c.color}-800/30` `` etc.), which Tailwind's JIT scanner can never see, so those color classes were silent no-ops — replaced with an explicit `ADVANTAGE_CATEGORY_STYLES` lookup of full literal class strings, matching the `MODULE_STYLES` pattern already used elsewhere. Added genuine cross-module connective content the old format had no place for: phase kickback's worked example ties back to the |−⟩ phase-detection example in `Measurement.jsx`, and the Shor section links forward to `/noise` for what "error-corrected logical qubit" costs in physical qubits. Fixed the same stale-completion-banner pattern as TASK-032/033. `GlossaryTooltip`/`Keyword` usage checked against real `glossary.js`/`Keyword.jsx` data. Verified: `npm run build` passes; grep confirms all 7 outline ids match section anchors, no legacy imports remain, exactly one `VideoAside` (now placed inside the Grover section it actually matches, versus its previous placement floating after the lesson stepper with no section association). Not yet visually verified in a browser. Labs (module 12) is next in sequence, not yet scoped. Completed 2026-08-17.

## Backlog (unordered, to be triaged)
(none currently)
